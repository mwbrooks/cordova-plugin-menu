#! /bin/sh

# Build Application for Android
# =============================

PROJECT_PATH=target/android

if [ ! -e ${PROJECT_PATH}/local.properties ] ; then cd ${PROJECT_PATH} && android update project -p . -t 1; fi
	
ant -f ${PROJECT_PATH}/build.xml debug install