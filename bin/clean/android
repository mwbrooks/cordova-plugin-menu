#! /bin/sh

# Clean Project for Android
# =========================

PROJECT_PATH=./target/android/assets/www

if test -d ${PROJECT_PATH}; then \
    rm -Rf ${PROJECT_PATH}; \
fi

mkdir -p ${PROJECT_PATH}
