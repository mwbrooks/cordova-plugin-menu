#! /bin/sh

# Clean Project for BlackBerry
# ============================

PROJECT_PATH=./target/blackberry-webworks/www

if test -d ${PROJECT_PATH}; then \
    rm -Rf ${PROJECT_PATH}; \
fi

mkdir -p ${PROJECT_PATH}
